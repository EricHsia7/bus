import { integrateLocation } from '../../data/apis/index.ts';
import { md5 } from 

export function openLocation(hash: string): void {

}

function refreshLocation(): object {
`r_${md5(Math.random() * new Date().getTime())}`
}